import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const AIScreen = () => {
  return (
    <ThemedView>
      <ThemedText>AI Screen</ThemedText>
    </ThemedView>
  );
};

export default AIScreen;
