import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

const StatusScreen = () => {
  return (
    <ThemedView>
      <ThemedText>Status Screen</ThemedText>
    </ThemedView>
  );
};

export default StatusScreen;
